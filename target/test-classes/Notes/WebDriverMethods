Methods:
--------
1.get methods
2.conditional methods
3.broswer methods
4.navigational methods
5.wait methods

get methods:
------------
1.get(url) - open the url in the browser
2.getTitle() - returns the title of the webpage
3.getCurrentUrl() - return the url of the page
4.getPageSource() - returns code of the page
5.getWindowHandle() - return the ID of the single browser window
6.getWindowHandles() - return the ID's of the Multiple the browser window


Conditional methods: can perform on top of web element
--------------------
it all returns T/F boolean value
1.isDisplayed() - check the element is displayed or not
2.isEnabled() - check the element is enabled or not
3.isSelected() - check the element is selected or not

Browser methods:
----------------
1.close() - close the one window(current)
2.quit() - close entire browser

wait methods: synchronization issue we are using wait method
-------------
selenium WebDriver wait:
-----------------------
1.implicit wait
2.explicit wait/fluent wait -  declare and then we can use wait

explicit wait syntax:
--------------------
WebDriverWait mywait=new WebDriverWait(driver,Duration.ofSeconds(5));
Java Wait:
---------
thread.sleep(1000)

Navigational Methods:
---------------------
1.navigate().to(url)
2.navigate().back()
3.navigate().forward()
4.navigate().refresh()

navigate.to(url) --  it accepts the url in both string and Url Object 
URL myurl=new URL("www.google.com")
navigate().to(myurl)
or navigate().to("www.aaaa.com")
				Vs 
get(url) - only accepts the url in string format
get("www.cccc.com")